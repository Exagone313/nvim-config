buffer:
* conform: more formatters? enable on availability with backups?
* formatexpr on filetype only (like with nvim-treesitter indentexpr)
lsp:
* review keymaps (most are never used, move to menu?)
* fallback servers?
* blink-cmp setup
menu:
* hide leader menu when clicking out of it
* close diffview when selecting git diff?
top:
* in dropbar, when opening directory with left click, it directly closes neo-tree popup on click release
vim:
* review listchars

---

# issues/ideas

major
* change repository name (nvim-config?)
* mirror on gitlab.com and gitlab.ewd.app (should it be kept on github as main repo as all plugins are there?)
* add a "cheat sheet" document for maps and commands I don't remember
* not easy to differentiate tabs with files with the same name
* autocompletion does no contain values that would be completed by ctrl+P at all
=> seem to work in markdown...
* need a strategy for looking for files (by name and/or content) while navigating directories in neotree and/or FzfLua

lesser
* https://github.com/ptdewey/yankbank-nvim or https://github.com/gbprod/yanky.nvim
=> yanky needs https://github.com/stevearc/dressing.nvim afaik
* better neotree controls:
=> replace left and right arrow keys on neotree by parent directory and open file/expand directory
=> show H, the key to show hidden files, at the end of hidden item line
=> if possible, disable visual and hide cursor (keep only the line)
* select a help page in FzfLua and then open it in a new tab (full window)
* search for selected text (escape it)
